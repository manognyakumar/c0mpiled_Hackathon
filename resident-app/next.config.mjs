/** @type {import('next').NextConfig} */
const nextConfig = {
  // Lingo.dev integration (optional - requires @lingo-dev/next package)
  // Uncomment when lingo-dev is installed:
  // compiler: {
  //   lingo: {
  //     sourceLocale: "en",
  //     targetLocales: ["ar"]
  //   }
  // }
};

export default nextConfig;
